# Agent Instructions

本仓库是短视频封面生成 Skill。用户提到“封面、视频封面、给视频配图、做封面图”并提供本地视频时，先完整阅读 `SKILL.md`，再执行流程。

## 不可跳过的门

- 先抽帧并亲自查看视频内容，写出可追溯的内容分析。
- 视频有真人时，在同一条消息中同时询问“人像策略”和“标题候选”，等待用户一次回复组合，例如 `2B`。
- 未确认标题和人像策略前，不生成最终封面；用户明确要求全自动时除外。
- 先生成三张真正不同的 3:4 路线，逐张质检后在对话中展示，不默认创建 HTML 评审页。
- 用户选中竖版后，才生成该 campaign 的原生 4:3 横版。不得裁切、拉伸或把其他路线改成横版。

## 两套模型适配器

- Codex / GPT Image 2：读取 `scripts/codex_showcase_prompt_builder.py` 生成的参考图角色和画面契约，追求 Showcase 上限。
- Dreamina / Seedream：复用同一内容决策，但使用短中文视觉配方。不要把 Image 2 长提示词直接翻译给 Dreamina。
- 需要准确品牌物时，只使用当前官方资产作为内容参考；不得从外部封面抠 Logo。
- 引擎、模型版本、参考图角色和最终选择必须如实记录，不得把泛 `5.0` 标成 `5.0 Pro`。

## 交付标准

缩略图第一眼必须读懂主题，第二眼看到结果或证据，第三眼才是人物和氛围。错字、叠字、断肢、人物漂移、三张同质化、课程海报感、横版标题过小都视为失败，只重做对应路线。

与用户使用中文交流。详细命令、状态门和验收标准以 `SKILL.md` 为准。
