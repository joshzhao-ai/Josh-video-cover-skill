interface:
  display_name: "Josh Video Cover Skill"
  short_description: "分析本地视频，生成可点击且跨比例一致的短视频封面"
  default_prompt: "Use $video-cover-generator to create a high-impact cover from this local video."

policy:
  allow_implicit_invocation: true
